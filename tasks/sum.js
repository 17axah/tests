/**
 * Напиши функцию, которая складывает два числа.
 *
 * Если в функцию не передали аргументы функция должна возвращать 0 (Подсказка: Параметры по умолчанию)
 * 
 * @param {number} a первое число
 * @param {number} b второе число
 */
function sum(a, b) {
  // code
}

module.exports = sum;
