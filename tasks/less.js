/**
 * Фукнция проверяет меньше ли число(value) чем второй аргумент(num)
 * Если меньше должна вернуть - true
 * Если больше или равно - false
 * 
 * @param {number} value первое число
 * @param {number} num второе число
 */
 function less(value, num) {
  // code
}

module.exports = less;
